{
    "version": 2,
    "builds": [
        {
            "api": "api/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "api": "(.*)",
            "dest": "api/server.js"
        }
    ]
}